CREATE TABLE dl_progress (ACCOUNT_NAME TEXT NOT NULL, VOLUME_ID TEXT NOT NULL, BOOK_ACCESS INTEGER NOT NULL, FORMAT INTEGER NOT NULL, VERSION TEXT NOT NULL, STORAGE_ID TEXT NOT NULL, PROGRESS INTEGER NOT NULL, UNIQUE (ACCOUNT_NAME, VOLUME_ID, BOOK_ACCESS, FORMAT, VERSION, STORAGE_ID) ON CONFLICT REPLACE);
